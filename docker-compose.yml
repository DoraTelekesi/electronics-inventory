version: "3.9"
services:
  mongo:
    image: mongo:7
    container_name: inventory-mongo-db
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
